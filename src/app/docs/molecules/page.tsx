export default function MoleculesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold" style={{ color: 'var(--color-emphasis)' }}>
        Molecules
      </h1>
      <p className="mt-2" style={{ color: 'var(--color-text-secondary)' }}>
        InputGroup, FormField, SearchBar, StatCard, ListItem, AlertMessage, ToastMessage,
        AccordionItem, DropdownItem, PaginationItem, BreadcrumbItem, Tab
      </p>
    </div>
  );
}
